namespace Ara.SmartHSE.Infrastructure.Tests.Persistence.Repositories
{
    public class AreaRepositoryTest
    {
        [Fact]
        public async Task qweqwe()
        {
            //Mock< SmartHSEContext> mock=new Mock< SmartHSEContext>();
            //var area=new Mock<DbSet<Area>>();
            //var area1 = new Area() { Name = "0" };

            ////area.Setup(s => s.AddAsync(It.IsAny<Area>())).Callback((Area a, CancellationToken token) => { })
            //    .Returns((Area a) => ValueTask.FromResult(area1));

            //mock.Setup(s=>s.Set<Area>()).Returns(area.Object);
            //mock.Setup(s => s.SaveChangesAsync(It.IsAny<CancellationToken>())).Returns(()=>Task.CompletedTask);

            //var rrrr = new AreaRepository(mock.Object);

            //var res = await rrrr.AddAsync(area1);

            //var r0 = 0;
            //var dbContext = new Mock<SmartHSEContext>();
            //employeeContextMock.Setup<DbSet<Area>>()
        }
    }
}
